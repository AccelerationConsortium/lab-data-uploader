# AWS credentials are provided via IAM role on ECS (no env vars needed).
# For local development, configure ~/.aws/credentials or set:
# AWS_ACCESS_KEY_ID=...
# AWS_SECRET_ACCESS_KEY=...
# AWS_DEFAULT_REGION=ca-central-1
